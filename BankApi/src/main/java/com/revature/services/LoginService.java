package com.revature.services;

public class LoginService {
	//This should keep track of the active user (through getActiveUser()) and should handle logging in
	//and logging out -- possibly also include registering
}
